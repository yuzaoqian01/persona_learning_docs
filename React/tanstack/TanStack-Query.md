# 安装

```cmd
pnpm add @tanstack/react-query
```

兼容性

```css
Chrome >= 91
Firefox >= 90
Edge >= 91
Safari >= 15
iOS >= 15
Opera >= 77
```

Eslint  插件

```cmd
pnpm add -D @tanstack/eslint-plugin-query
```

